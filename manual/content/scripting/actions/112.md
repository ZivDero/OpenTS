---
type: action
id: TACTION_ENABLE_ALLY_REVEAL
title: Enable Ally Reveal
summary: Turns ally reveal on, so the player also sees what allied houses reveal.
caveats:
  - The setting is the one `AllyReveal=` initializes. It lasts for the rest of the scenario and through its saves, and the next scenario reads the key afresh.
related:
  - type: action
    id: TACTION_DISABLE_ALLY_REVEAL
  - type: key
    id: AllyReveal
  - type: system
    id: map-visibility
---

The key's page owns what ally reveal shows; the action only switches it on for the scenario in progress.
