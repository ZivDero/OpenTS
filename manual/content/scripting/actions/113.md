---
type: action
id: TACTION_DISABLE_ALLY_REVEAL
title: Disable Ally Reveal
summary: Turns ally reveal off, so the player no longer sees what allied houses reveal.
caveats:
  - The setting is the one `AllyReveal=` initializes. It lasts for the rest of the scenario and through its saves, and the next scenario reads the key afresh.
  - Terrain an ally has already revealed stays revealed; only what allies reveal from now on is withheld.
related:
  - type: action
    id: TACTION_ENABLE_ALLY_REVEAL
  - type: key
    id: AllyReveal
  - type: system
    id: map-visibility
---

The key's page owns what ally reveal shows; the action only switches it off for the scenario in progress.
