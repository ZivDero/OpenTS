---
type: action
id: TACTION_ENABLE_SHORT_GAME
title: Enable Short Game
summary: Turns the short game rule on, so a house in a skirmish or multiplayer game is defeated once it has no structures and no base unit left.
caveats:
  - Every house is checked against the rule on each pass of its own logic, so a house already down to no structures and no base unit is defeated as soon as its next pass runs.
  - A campaign never applies the rule, so the action changes nothing there.
related:
  - type: action
    id: TACTION_DISABLE_SHORT_GAME
  - type: format
    id: spawn-ini
---

The rule is the one the lobby's Short Game option sets before the match, so the action changes the same setting the players agreed on.
